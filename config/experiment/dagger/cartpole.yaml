# @package _global_
defaults:
  - override /services/actor:
      - dagger_teacher
      - dagger_learner
  - override /run: dagger
  - override /services/environment: cartpole
run:
  teacher_model: SimpleA2CModel
  teacher_model_id: naughty_wiles_0_model
