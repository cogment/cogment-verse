# @package _global_
defaults:
  - override /services/environment: pong_pz
  - override /services/trial_datastore: local
  - override /run/experiment_tracker: simple

run:
  class_name: runs.play.PlayRun
  num_trials: 5
  seed: 12
  players:
    - name: player_1  # Left player
      implementation: client
    - name: player_2  # Right player
      implementation: actors.random_actor.RandomActor
